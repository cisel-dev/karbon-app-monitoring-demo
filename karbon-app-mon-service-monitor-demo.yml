apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: monitoring-apps
  namespace: default
  labels:
    monitoring: apps
spec:
  selector:
    matchLabels:
      monitoring: apps
  endpoints:
    - port: http-metrics
